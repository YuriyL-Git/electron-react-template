import { createStyles } from '../../common/helpers/create-styles';

export const styles = createStyles({
  container: {
    height: '45px',
    background: 'none',
    marginLeft: '20px',
    marginBottom: '20px',
  },
});
